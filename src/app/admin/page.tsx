// This page is handled by the layout.tsx file above
// The layout will show the welcome screen when pathname is '/admin'

export default function AdminPage() {
  return null; // Content is rendered in layout.tsx
}